# DL_Assignment2
Multiclass image classification using Convolutional Neural Networks (CNNs) identifies and categorizes images into multiple classes by automatically learning visual features such as edges, shapes, and textures. **Transfer learning** uses a pre-trained model to improve accuracy and reduce training time, making it effective even with smaller datasets.
